version: '3.8'

services:
  app:
    image: ghcr.io/corp-momenti/docker-images/momenti-elixir-dev:8339e0facd983766f92588b50e1326799090a90a
    volumes:
      - ..:/home/vscode/workspace:cached
      - build:/home/vscode/workspace/_build
      - deps:/home/vscode/workspace/deps
      - config:/home/vscode/.config
      - extensions:/home/vscode/.vscode-server/extensions
      - local:/home/vscode/.local
    command: sleep infinity
    init: true
    privileged: true
    network_mode: service:postgres
    environment:
      WARNINGS_AS_ERRORS: 'false'

volumes:
  config:
  build:
  deps:
  extensions:
  local:
